public class Operadores
{
	public static void main(String[] args)
	{
		//Denominação
		//Unários, Binários, Ternários
		//Expressão
		// Operandos: 2 e 3 | Operador: +
		//2 + 3  //Binário
		//true ? "Sim" : "Não" // Operador Ternário

		int x = 9 + 4;


		/*Operador          Finalidade

		+ - * / %			Soma, subtrai, multiplica, divide, módulo ( que retorna o resto da divisão por um numero)
		+					Concatenação de strings
		+ -					Operador unário mais e menos
		< > <= >=			Comparar, menor que, maior que, menor ou igual, maior ou igual
		++,--				Pré ou pós incremento, pré ou pós decremento
		&&, ||				Concatenação lógica e, concatenação lógica ou
		.					Acessar propriedades de um objeto
		[]					Indexar arrays
		()					Chamar método
		?: 					Ternário, retorna booleano em uma 
		<< >> >>>			Operar bit númerico
		~					Complemento unário sobre bits
		!					Complemento lógico
		& | ^				Operadores bit AND, OR e XoR
		,					Separar valores
		=					Atribuir valor
		+= -= *= /= 		Atribuir valores somando, subtraindo, multiplicando, dividindo
		<<= >>= >>>=		Operador de bytes (unário)
		&= |= ^=			Operador de bytes AND, OR , XoR (unário)
		==					Comparação de valores
		instanceof			Verifica o tipo do objeto
		new					Criar objeto
		(tipo)				Coerção unária

		*/
	}	

}